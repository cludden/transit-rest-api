'use strict';

const expect = require('chai').expect;

describe('basic tests', function() {
    it('should correctly parse all queries', function() {
        let tests = [{
            query: {
                filter: {
                    type: 'expense-report'
                }
            }
        }];
    });
});
